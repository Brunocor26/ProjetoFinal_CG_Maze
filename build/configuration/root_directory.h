// Path relative to the executable (assumes executable is in the `build/` dir)
// Use an environment variable `LOGL_ROOT_PATH` to override at runtime if needed.
// Path relative to the executable (assumes executable is in the `build/` dir)
// Can be overridden at runtime with the LOGL_ROOT_PATH env var.
const char *logl_root = "..";
